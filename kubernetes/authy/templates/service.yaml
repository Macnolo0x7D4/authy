apiVersion: v1
kind: Service
metadata:
  name: authy-service
  namespace: {{ default "authy" .Release.Namespace }}
spec:
  ports:
    - port: 4000
      protocol: TCP
      targetPort: http
      name: http
  selector:
    app: authy
  type: ClusterIP
